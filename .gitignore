# Claude
.claude/plans/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/

# uv
.uv/

# Environment
.env
.env.local

# Testing / tooling caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# Data artifacts (scraped / enriched datasets, raw caches).
# Anchored to the repo root so it does NOT match the src/.../data/ package.
# Glob the contents (not the dir) so the curated golden set can be re-included.
/data/*
*.jsonl

# The golden eval set is curated test data, not a generated artifact — keep it
# version-controlled. Re-included after the /data/ and *.jsonl ignores above.
!/data/golden_set.jsonl

# Bottle photos: imported by hand from the owner's browser (Cloudflare blocks
# server-side fetch) and served first-party from /bottles/<SKU>.jpg. Kept out of
# git as a local/deploy asset — ~260KB each, ~100MB at full catalogue coverage.
# Re-populate with: python data/import_images.py
src/hedonism_assistant/api/static/bottles/

# Qdrant local storage
qdrant_storage/

# Logs
*.log

# IDE
.idea/
.vscode/

# OS
.DS_Store
